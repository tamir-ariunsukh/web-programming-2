# AGENTS.md — AI агентуудад зориулсан заавар

Энэ repo дээр **код бичих, засах бүх AI агент** энэ файлыг дагаж ажиллана.
Хүнд зориулсан тайлбар нь [`README.md`](README.md), [`GITHUB.md`](GITHUB.md),
`weekN/README.md` дотор байна.

> **Дүрэм №0:** Ажил эхлэхийн өмнө холбогдох `weekN/README.md`-г УНШИНА. Тэнд тухайн
> долоо хоногийн сэдэв, дасгалын жагсаалт, хичээлийн харгалзаа, шалгах зүйлс бий.
> Шинэ долоо хоног нэмж байгаа бол өмнөх долоо хоногийн README-г жишээ болгоно.

Конвенц өөрчлөгдвөл (шинэ чадвар, шинэ нэмэлт сан, өөр бүтэц) **энэ файлыг ч бас
шинэчилнэ** — агентууд энд бичсэнийг л мөрддөг.

---

## 1. Repo гэж юу вэ

Хүрээ их сургуулийн **Web Programming II** хичээлийн лабораторын материалууд.
**Хичээлийн код долоо хоног бүрээр (7 хоног тутам) нэмэгддэг**: `week3/`, `week4/`,
дараа нь `week5/`, `week6/`, ...

- Долоо хоног бүр нь **бие даасан** Vite 8 + React 19 төсөл — өөрийн `package.json`,
  `node_modules`, `vite.config.js`, `index.html`, `README.md`-тэй. Нэг долоо хоног
  нөгөөгөөсөө хамааралгүй ажиллана (`cd weekN && npm run dev`).
- Бүх код, тайлбар, баримт бичиг **монгол хэлээр**.
- Код бүр **мөр мөрөөрөө тайлбарласан** — тэр тайлбар нь хичээлийн агуулга.

---

## 2. Хавтасны бүтэц

```
web-programming-2/
├─ README.md      # Долоо хоногуудын жагсаалт (хүнд зориулсан)
├─ GITHUB.md      # git/gh тушаалын гарын авлага
├─ AGENTS.md      # энэ файл (агентад зориулсан)
├─ week3/         # State ба үйл явдал (useState, үйл явдал, форм, нөхцөлт ба
│                 # жагсаалтын дүрслэл, төлөв өргөх, localStorage)
└─ week4/         # useEffect (mount/update/unmount, cleanup, fetch)
```

`weekN/` доторх бүтэц (жишиг: `week3/`):

```
weekN/
├─ index.html
├─ vite.config.js
├─ package.json
├─ README.md              # тухайн долоо хоногийн гарын авлага (6-р хэсгийг үз)
├─ public/                # шаардлагатай бол (ж: week4/public/students.json)
└─ src/
   ├─ main.jsx            # эхлэл (entry)
   ├─ index.css           # нийтлэг хэв маяг + код хайрцгийн хэв маяг
   ├─ App.jsx             # цэс + сонгосон дасгал + эх код
   ├─ components/
   │  ├─ CodeBlock.jsx    # эх кодыг харуулах ба засварлах (4.1, 4.2)
   │  └─ LivePreview.jsx  # алдааны хамгаалалт (Error Boundary, класс)
   ├─ lib/
   │  └─ compileLab.js    # JSX-ийг browser дотор ажиллуулах (Babel)
   └─ labs/
      ├─ 01-Нэр.jsx       # нэг файл = нэг сэдэв, нэг <section className="card">
      └─ ...
```

---

## 3. Долоо хоног нэмэх дараалал

1. **Суурь:** өмнөх `week<N-1>`-ийг хуулна (эсвэл Vite-ийн react загвараас эхэлнэ).
   `node_modules/`, `dist/`, `package-lock.json`-ыг хуулахгүй. `package.json`-ы
   `name`-ийг `week<N>-…` болгоно.
2. **Туслах файлуудыг заавал авна:** `src/components/CodeBlock.jsx`,
   `src/components/LivePreview.jsx`, `src/lib/compileLab.js`, `index.css`-ийн сүүлийн
   `.code-*` ба `.live-*` хэсэг, `package.json`-д `@babel/standalone` сан. Эдгээр нь
   4-р хэсгийн 2 чадварыг хангана — **устгах, сулруулахгүй**.
3. **Дасгалууд:** `src/labs/NN-Нэр.jsx` — тухайн долоо хоногийн сэдвээр, мөр бүр
   монгол тайлбартай, `export default function Нэр() { ... }`.
4. **App.jsx:** `LABS` массивт `{ id, title: '1. …', View, file: '01-Нэр.jsx' }`.
   `file` талбар **заавал** — эх кодыг харуулах/засварлахад хэрэгтэй.
5. **README:** `weekN/README.md`-г 6-р хэсгийн бүтцээр бичнэ.
6. **Root README:** "Долоо хоногууд" хүснэгтэд шинэ мөр нэмнэ.
7. **Шалгаад** (7-р хэсэг) commit/push хийнэ — журмыг [`GITHUB.md`](GITHUB.md)-ээс.

---

## 4. ЗААВАЛ байх 2 UI чадвар

Энэ 2 чадвар нь **долоо хоног бүрийн төсөлд** байх ёстой (оюутан кодоо дэлгэцэн
дээрээс харж, засаж, үр дүнг нь тэр дор нь харна). Жишиг хэрэгжүүлэлт: `week3/`,
дэлгэрэнгүй тайлбар: `week3/README.md` §6.

| Долоо хоног | 4.1 Эх код | 4.2 Амьд засвар |
| --- | --- | --- |
| `week3/` | ✅ бий | ✅ бий |
| `week4/` | ❌ — нэмэх | ❌ — нэмэх |

### 4.1. Дасгал бүрийн эх кодыг дэлгэцэн дээр харуулах

- Эх текст нь `import.meta.glob`-оор уншигдана (Vite-ийн боломж, `?raw` = файлыг
  compile хийхгүй, текстээр):

  ```js
  const SOURCES = import.meta.glob('./labs/*.jsx', {
    query: '?raw',
    import: 'default',
    eager: true,
  })
  ```

- `CodeBlock` props: `fileName`, `code`, `editedCode`, `onRun`, `onReset`; App дээр
  `key={activeLab.id}` байрлуулна (дасгал солиход дотоод төлөв шинээр эхэлнэ).
- Хайрцагт харагдах код нь **ажиллаж буй кодтой ижил** байх ёстой (`prepare(draft)`),
  толгойн шошго 3 төлөвтэй: `Засварлагч` / `Засвар` / `Эх код`.
- Мөр дугаар, өнгөт токен (`.tok-*`), `Хуулах`, `Код нуух`/`Код харуулах` хэвээр.

### 4.2. Амьд засвар: код → DOM

Гинжин урсгал (бүх холбоос хэвээр байх ёстой):

```
textarea (CodeBlock) ──0.6с debounce (useEffect + setTimeout)──▶ onRun(code)
     └▶ App.handleRun ──▶ compileLab(code)  [Babel: JSX→JS, new Function]
            ├─ амжилт: edits[labId] = { code, Component, error: null, run }
            └─ алдаа : edits[labId] = { code, Component: null, error, run }
     └▶ демо: <ActiveView />-ийн оронд
              <LivePreview key={edit.run} error={edit.error}><EditedView /></LivePreview>
```

- `Буцаах` → `CodeBlock.handleResetClick` (`setDraft`/`setLastRun`) + `App.handleReset`
  (`edits`-ээс тухайн lab-ийг устгана) → эх компонент буцаж харагдана.
- Babel-ийг **зөвхөн хэрэглэгч засвар хийх үед** динамик `import()`-оор татна —
  эх bundle-д орохгүй (build дээр тусдаа chunk).
- `compileLab.js` доторх Babel-ийн тохиргоог өөрчлөхгүй:
  `presets: [['react', { runtime: 'classic' }]]`, `plugins: ['transform-modules-commonjs']`
  (JSX → `React.createElement`, `import/export` → `require/exports`).
- Демо дээр алдаагүй үед цэнхэр мөр (`.live-bar`) гарч «таны засвар ажиллаж байна»
  гэж мэдэгдэнэ; алдаа гарвал `.live-error` улаан хайрцагт мессеж гарна (хуудас
  бүхэлдээ унахгүй — үүнийг `LivePreview` класс компонент хариуцна).
- Ажиллах бүрд компонент шинэ функц болдог тул төлөв эхнээс эхэлнэ (зөв зан төлөв,
  оюутанд тайлбарлаж өгнө).

---

## 5. Кодын хэв маяг

- **Тайлбар монгол хэлээр, мөр мөрөөр** — юу хийж байгааг биш, ЯАГААД гэдгийг
  тайлбарлана. Байгаа тайлбарыг устгах, товчлох, англи болгохгүй.
- Нэг файл = нэг сэдэв. Компонент `PascalCase`, файл `NN-Нэр.jsx`, үндсэн элемент нь
  нэг `<section className="card">`.
- `labs/` доторх дасгалууд **зөвхөн тухайн долоо хоногийн сэдвээр** байна (ж: `week3`-д
  `useEffect` бичихгүй). Дараагийн долоо хоногийн ойлголтыг зөвхөн `components/`,
  `lib/` туслах файлд, «энэ бол дараагийн долоо хоногийн сэдэв» гэсэн тайлбартай
  ашиглаж болно.
- Нэмэлт сан (dependency) нэмэхээс өмнө хэрэглэгчээс асууна. Одоогоор зөвшөөрөгдсөн
  нэмэлт: `@babel/standalone` (зөвхөн амьд засварт). Tailwind гэх мэт CSS framework,
  TypeScript нэмэхгүй.
- Өнгийг `index.css`-ийн `:root` хувьсагчаар солино; өмнөх долоо хоногийн хэв маягийг
  үргэлжлүүлнэ (шинээр зохиохгүй).

---

## 6. Баримт бичгийн (README) хэв маяг

`weekN/README.md` бүтэц (жишээ: `week3/README.md`):

| # | Хэсэг |
| --- | --- |
| 1 | Ажиллуулах (тушаалын хүснэгттэй) |
| 2 | Файлын бүтэц (мод хэлбэрээр) |
| 3 | Хичээлийн хэсэг ↔ файлын харгалзаа (хүснэгт) |
| 4 | Дадлага даалгаврын хариу код хаана байна |
| 5 | Турших зүйлс (багшийн демо) — дугаарласан жагсаалт |
| 6 | Эх код: харах ба засварлаж ажиллуулах (6.1 Харах, 6.2 Амьд засвар) |
| 7 | Оюутнуудын түгээмэл алдаа (сануулга) |
| 8+ | Тухайн долоо хоногийн нэмэлт хэсгүүд (нэмэлт дасгал, дараагийн долоо хоног руу гүүр г.м.) |

- `week4/README.md` нь өөр бүтэцтэй (жагсаалт, шалгах хуудас, гэрийн даалгавар) —
  аль нэгийг нь сонгоод **тухайн долоо хоногийнхоо хэв маягийг үргэлжлүүлнэ**.
- Root [`README.md`](README.md)-ийн "Долоо хоногууд" хүснэгтэд мөр нэмнэ.
- Баримт бичгийг давхардуулахгүй: дэлгэрэнгүйг `weekN/README.md`-д, энэ файлд зөвхөн
  дүрэм, конвенцийг бичнэ.

---

## 7. Шалгалт (заавал хийх)

1. `npm run build` — алдаагүй дуусах. Babel нь тусдаа chunk болж гарна (~2.2MB,
   563KB gzip) — энэ хэвийн; «chunk > 500kB» гэсэн анхааруулгыг үл тоомсорлоно.
2. `npm run dev` + browser tools. **Чухал:** screenshot-ийн OCR монгол кириллийг
   буруу уншдаг тул харагдацыг `page.evaluate`-ээр DOM-оос шалгана.
3. Шалгах жагсаалт:
   - [ ] Дасгал бүрийн хайрцаг дахь код эх файлтай **тэмдэгт тус бүрээр** тэнцэнэ.
   - [ ] `Засварлах` → 0.6 секундын дараа дээрх демо шинэчлэгдэнэ (жишээ нь товчны
         шошго солигдох), цэнхэр `.live-bar` гарна.
   - [ ] Compile алдаа (таг хаагаагүй) ба ажиллагааны алдаа (ж: `x.hello()`) →
         улаан хайрцаг; хуудсын бусад хэсэг ажилласаар байна.
   - [ ] `Буцаах` → эх код, эх демо буцаж ирнэ.
   - [ ] Дасгал солиод буцахад засвар хадгалагдсан, editor зөв текстээр эхэлсэн.
   - [ ] Console-д warning/error байхгүй (React DevTools-ийн мэдээллээс бусад).
   - [ ] Нарийн дэлгэцэд (`760px`-ээс доош) багтана.

---

## 8. Командууд

```powershell
Set-Location 'c:\Users\atamirpc\Documents\GitHub\web-programming-2\week3'
npm install        # зөвхөн эхний удаа
npm run dev        # http://localhost:5173
npm run build      # шалгалт
npm run preview    # http://localhost:4173
```

- Энэ орчинд `npm` нь **pnpm** руу заасан — зан төлөв ижил.
- Терминалын ажлын хавтас алдагдаж болзошгүй тул `Set-Location`-ийг тушаалтайгаа
  НЭГ мөрөнд бичнэ.

---

## 9. Хийхгүй зүйлс

- Тайлбарыг устгах/орчуулах, дасгалын кодыг «цэвэрлэх», богиносгох.
- 4-р хэсгийн 2 чадварыг (эх код, амьд засвар) арилгах, идэвхгүй болгох, өөр
  хэлбэрт шилжүүлэх.
- Хүсэлтгүйгээр шинэ сан, CSS framework, TypeScript нэмэх.
- Долоо хоног хооронд код «зөөх»: төсөл бүр бие даасан. Зөвхөн 3.2-т заасан туслах
  файлуудыг хуулна.
- Файлын нэрийг өөрчлөх (`AGENTS.md`, `GITHUB.md`, `README.md`, `NN-Нэр.jsx` хэвээр).
